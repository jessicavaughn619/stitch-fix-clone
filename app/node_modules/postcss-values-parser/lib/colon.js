'use strict';

const Container = require('postcss-values-parser/lib/container');
const Node = require('postcss-values-parser/lib/node');

class Colon extends Node {
  constructor (opts) {
    super(opts);
    this.type = 'colon';
  }
}

Container.registerWalker(Colon);

module.exports = Colon;
